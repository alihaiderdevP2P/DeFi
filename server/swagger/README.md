# Swagger

Swagger docs are mounted at:

- `/api/docs`
- Health endpoint (documented in Swagger): `/health`

No static swagger file is needed because documentation is generated at runtime from decorators.
